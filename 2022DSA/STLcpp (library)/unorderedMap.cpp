#include<iostream>
#include<unordered_map>
using namespace std;

// Unordered Map are Hashtable
// best About Map --> Insertion(),erase(),find() have O(1) in Avg. Case and 0(n) other Case

int main()
{
    //similar to map but in unordered_map values or keys are accesed by hash table
    // when not necessary to store in ordered way use unordered_maps --
}